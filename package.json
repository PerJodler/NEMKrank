{
  "name": "vitachronik-design-01-dopamin-pop",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Installierbare Offline-Gesundheitschronik für Krankheitsverlauf, Arzttermine und NEM-Planung.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx --yes wrangler@4 dev",
    "deploy": "npm run check && npm run build && npx --yes wrangler@4 deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
